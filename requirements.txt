# Cœur traitement de signal
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Analyse ECG (R-peaks, FC, HRV, délimitation P-QRS-T)
neurokit2>=0.2.7
pandas>=2.0          # dépendance de neurokit2, listée explicitement

# Tests
pytest>=8.0

# Parsing .atc — repli optionnel sur l'implémentation tierce.
# L'implémentation maison dans atc_reader.py fonctionne sans, mais pyATC
# couvre davantage de variantes de format.
# pyATC>=1.0.6
